{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/securedocs-dashboard run dev",
    "dev:dashboard": "pnpm --filter @workspace/securedocs-dashboard run dev",
    "dev:mockup": "pnpm --filter @workspace/mockup-sandbox run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "watch:audit": "mongosh \"mongodb+srv://harshpanchal200011_db_user:xVN3cfRbKmNSIOWA@sih.9ut1ht1.mongodb.net/securedocs\" --quiet scripts/watch-audit.js",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
