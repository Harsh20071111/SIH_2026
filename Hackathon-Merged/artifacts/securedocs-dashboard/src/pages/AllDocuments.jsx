import { useEffect, useMemo, useState } from 'react';
import { AlertTriangle, ArrowDown, ArrowUp, CheckCircle2, ChevronDown, ChevronLeft, ChevronRight, CircleHelp, Download, Eye, FileCheck2, Filter, History, KeyRound, Link2, Loader2, LogOut, MoreHorizontal, Plus, RefreshCw, Search, Share2, Shield, ShieldAlert, SlidersHorizontal, Upload, UserRound, X } from 'lucide-react';
import { documents as seedDocuments, filterOptions } from '@/data/documents';
import { DetailIcon, Dropdown, EmptyState, IconButton, Modal, SkeletonRows, StatusBadge, Toast } from '@/components/SecureDocsComponents';

const formatDate = (date) => new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(String(date).includes('T') ? date : `${date}T12:00:00`));
const formatDateTime = (date) => new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit' }).format(new Date(date));
const unique = (key) => [...new Set(seedDocuments.map((doc) => doc[key]))];

function SelectField({ label, value, onChange, options, testId }) {
  return <label className="min-w-0 flex-1"><span className="mb-1.5 block text-[10px] font-extrabold uppercase tracking-[.12em] text-muted-foreground">{label}</span><div className="relative"><select value={value} onChange={(event) => onChange(event.target.value)} data-testid={testId} className="h-9 w-full appearance-none rounded-md border border-input bg-card px-2.5 pr-7 text-xs font-semibold text-foreground outline-none transition focus:border-primary"><option value="">All {label}</option>{options.map((option) => <option key={option} value={option}>{option}</option>)}</select><ChevronDown size={13} className="pointer-events-none absolute right-2.5 top-3 text-muted-foreground" /></div></label>;
}

function SummaryCard({ label, value, detail, icon: Icon, tone = 'navy', testId }) {
  const toneClass = {
    navy: 'bg-secondary text-primary',
    teal: 'bg-[#16803C]/10 text-[#16803C]',
    amber: 'bg-[#B77900]/10 text-[#B77900]',
    red: 'bg-[#C62828]/10 text-[#C62828]',
  }[tone];
  return <div className="rounded-lg border border-border bg-card px-4 py-4 shadow-sm" data-testid={testId}><div className="flex items-start justify-between"><div><div className="text-[11px] font-bold uppercase tracking-[.12em] text-muted-foreground">{label}</div><div className="mt-1 text-2xl font-extrabold tracking-tight text-foreground">{value}</div></div><div className={`flex h-9 w-9 items-center justify-center rounded-md ${toneClass}`}><Icon size={18} /></div></div><div className="mt-3 text-[11px] font-medium text-muted-foreground">{detail}</div></div>;
}

function DocumentRow({ document, onMenu, onPreview, onDetails }) {
  return <tr className="group border-t border-border transition-colors hover:bg-muted/40" data-testid={`row-document-${document.id}`}>
    <td className="w-9 px-3 py-3.5"><button type="button" aria-label={`Select ${document.documentName}`} data-testid={`checkbox-document-${document.id}`} className="h-4 w-4 rounded border border-input bg-card hover:border-primary" onClick={() => {}} /></td>
    <td className="min-w-[265px] px-3 py-3.5"><button type="button" onClick={() => onDetails(document)} data-testid={`button-open-document-${document.id}`} className="flex items-start gap-3 text-left"><span className="mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-md bg-secondary text-primary"><DetailIcon type={document.documentType} /></span><span><span className="block text-[13px] font-extrabold leading-5 text-foreground group-hover:text-primary">{document.documentName}</span><span className="font-mono-app mt-0.5 block text-[10px] text-muted-foreground">{document.id} · {document.documentType}</span></span></button></td>
    <td className="whitespace-nowrap px-3 py-3.5"><span className="font-mono-app text-[11px] font-medium text-foreground" data-testid={`text-case-id-${document.id}`}>{document.caseId}</span></td>
    <td className="whitespace-nowrap px-3 py-3.5"><span className="text-xs font-semibold text-foreground" data-testid={`text-document-type-${document.id}`}>{document.documentType}</span></td>
    <td className="whitespace-nowrap px-3 py-3.5"><span className="text-xs font-semibold text-foreground" data-testid={`text-uploaded-by-${document.id}`}>{document.uploadedBy}</span></td>
    <td className="whitespace-nowrap px-3 py-3.5"><span className="text-xs font-semibold text-muted-foreground" data-testid={`text-upload-date-${document.id}`}>{formatDate(document.uploadDate)}</span></td>
    <td className="px-3 py-3.5"><span className="font-mono-app rounded-md bg-secondary px-2 py-1 text-[11px] font-medium text-secondary-foreground" data-testid={`text-version-${document.id}`}>v{document.version}</span></td>
    <td className="px-3 py-3.5"><StatusBadge value={document.status} kind="status" /></td>
    <td className="px-3 py-3.5"><StatusBadge value={document.integrity} kind="integrity" /></td>
    <td className="px-3 py-3.5"><StatusBadge value={document.confidentiality} kind="confidentiality" /></td>
    <td className="whitespace-nowrap px-3 py-3.5"><div className="text-xs font-semibold text-foreground">{formatDate(document.lastModified)}</div><div className="font-mono-app mt-0.5 text-[10px] text-muted-foreground">{document.totalAccesses} accesses</div></td>
    <td className="px-3 py-3.5"><div className="relative flex justify-end gap-1"><IconButton label={`Preview ${document.documentName}`} onClick={() => onPreview(document)} testId={`button-preview-document-${document.id}`} className="h-7 w-7 text-muted-foreground hover:text-primary"><Eye size={15} /></IconButton><IconButton label={`More actions for ${document.documentName}`} onClick={() => onMenu(document.id)} testId={`button-actions-document-${document.id}`} className="h-7 w-7 text-muted-foreground hover:text-foreground"><MoreHorizontal size={16} /></IconButton>{onMenu.activeId === document.id && <div className="absolute right-0 top-8 z-20 w-48 overflow-hidden rounded-md border border-border bg-popover py-1 text-left shadow-xl" data-testid={`menu-document-${document.id}`}><button type="button" onClick={() => onPreview(document)} data-testid={`menu-preview-${document.id}`} className="flex w-full items-center gap-2 px-3 py-2 text-xs font-semibold text-foreground hover:bg-muted"><Eye size={14} />View document</button><button type="button" onClick={() => onDetails(document)} data-testid={`menu-details-${document.id}`} className="flex w-full items-center gap-2 px-3 py-2 text-xs font-semibold text-foreground hover:bg-muted"><FileCheck2 size={14} />View details</button><button type="button" onClick={() => onDetails(document)} data-testid={`menu-version-history-${document.id}`} className="flex w-full items-center gap-2 px-3 py-2 text-xs font-semibold text-foreground hover:bg-muted"><History size={14} />View version history</button><button type="button" onClick={() => onMenu.download(document)} data-testid={`menu-download-${document.id}`} className="flex w-full items-center gap-2 px-3 py-2 text-xs font-semibold text-foreground hover:bg-muted"><Download size={14} />Download</button><button type="button" onClick={() => onMenu.share(document)} data-testid={`menu-share-${document.id}`} className="flex w-full items-center gap-2 px-3 py-2 text-xs font-semibold text-foreground hover:bg-muted"><Share2 size={14} />Share</button><div className="my-1 border-t border-border" /><button type="button" onClick={() => onMenu.verify(document)} data-testid={`menu-verify-${document.id}`} className="flex w-full items-center gap-2 px-3 py-2 text-xs font-semibold text-primary hover:bg-secondary"><Shield size={14} />Verify integrity</button></div>}</div></td>
  </tr>;
}

function UploadModal({ onClose, onUpload }) {
  const [name, setName] = useState('');
  const [caseId, setCaseId] = useState('');
  const [type, setType] = useState('FIR');
  const [confidentiality, setConfidentiality] = useState('Confidential');
  const [fileName, setFileName] = useState('');
  return <Modal title="Add document to evidence control" eyebrow="Secure intake" onClose={onClose} testId="modal-upload-document">
    <form onSubmit={(event) => { event.preventDefault(); onUpload({ name: name || 'Untitled case document', caseId: caseId || 'CASE-2026-NEW', type, confidentiality, fileName: fileName || 'secure-document.pdf' }); }} className="space-y-4 px-6 py-5">
      <div className="rounded-md border border-primary/20 bg-secondary p-3 text-xs leading-5 text-primary"><ShieldCheckText /> AES-256 Encryption · SHA-256 Integrity Hash · Audit Logging</div>
      <label className="block"><span className="mb-1.5 block text-xs font-bold text-foreground">Document name</span><input value={name} onChange={(event) => setName(event.target.value)} data-testid="input-upload-document-name" placeholder="e.g. Probable Cause Affidavit" className="h-10 w-full rounded-md border border-input bg-card px-3 text-sm text-foreground outline-none focus:border-primary" /></label>
      <div className="grid grid-cols-2 gap-3"><label><span className="mb-1.5 block text-xs font-bold text-foreground">Case ID</span><input value={caseId} onChange={(event) => setCaseId(event.target.value)} data-testid="input-upload-case-id" placeholder="CASE-2026-0000" className="h-10 w-full rounded-md border border-input bg-card px-3 font-mono-app text-xs text-foreground outline-none focus:border-primary" /></label><label><span className="mb-1.5 block text-xs font-bold text-foreground">Document Type</span><select value={type} onChange={(event) => setType(event.target.value)} data-testid="select-upload-document-type" className="h-10 w-full rounded-md border border-input bg-card px-3 text-xs text-foreground outline-none focus:border-primary">{filterOptions.documentType.map((item) => <option key={item}>{item}</option>)}</select></label></div>
      <label className="block"><span className="mb-1.5 block text-xs font-bold text-foreground">Confidentiality Level</span><select value={confidentiality} onChange={(event) => setConfidentiality(event.target.value)} data-testid="select-upload-confidentiality" className="h-10 w-full rounded-md border border-input bg-card px-3 text-xs text-foreground outline-none focus:border-primary">{filterOptions.confidentiality.map((item) => <option key={item}>{item}</option>)}</select></label>
      <label className="block"><span className="mb-1.5 block text-xs font-bold text-foreground">Source file</span><div className="flex h-20 items-center justify-center rounded-md border border-dashed border-border bg-muted/30"><input type="file" onChange={(event) => setFileName(event.target.files?.[0]?.name || '')} data-testid="input-upload-file" className="w-[230px] text-xs text-muted-foreground file:mr-2 file:rounded-md file:border-0 file:bg-secondary file:px-2.5 file:py-1.5 file:text-xs file:font-bold file:text-primary" /></div>{fileName && <span data-testid="text-upload-file-name" className="mt-1 block text-[11px] text-[#16803C]">{fileName} ready for secure intake</span>}</label>
      <div className="flex justify-end gap-2 border-t border-border pt-4"><button type="button" onClick={onClose} data-testid="button-cancel-upload" className="rounded-md px-3 py-2 text-xs font-bold text-muted-foreground hover:bg-muted">Cancel</button><button type="submit" data-testid="button-confirm-upload" className="inline-flex items-center gap-2 rounded-md bg-primary hover:bg-[#123A61] px-4 py-2 text-xs font-bold text-primary-foreground shadow-sm transition-colors"><Upload size={14} />Secure upload</button></div>
    </form>
  </Modal>;
}

function ShieldCheckText() { return <span className="mr-1 inline-flex items-center gap-1 font-bold"><Shield size={13} /> Intake protection:</span>; }

function PreviewModal({ document, onClose, onDetails }) {
  return <Modal title={document.documentName} eyebrow={`Document preview · ${document.id}`} onClose={onClose} width="max-w-3xl" testId="modal-preview-document">
    <div className="grid gap-5 p-6 md:grid-cols-[1fr_250px]"><div className="flex min-h-[355px] items-center justify-center rounded-lg border border-border bg-background/80 p-5"><div className="flex h-[285px] w-full max-w-[470px] flex-col rounded-md border border-border bg-card px-9 py-7 shadow-sm"><div className="mb-5 flex items-center justify-between border-b border-border pb-4"><div className="h-3 w-32 rounded bg-muted" /><div className="h-5 w-5 rounded bg-secondary" /></div><div className="space-y-3"><div className="h-2 w-5/6 rounded bg-muted/60" /><div className="h-2 w-full rounded bg-muted/60" /><div className="h-2 w-4/5 rounded bg-muted/60" /><div className="mt-6 h-16 w-full rounded bg-muted/30" /><div className="h-2 w-3/4 rounded bg-muted/60" /><div className="h-2 w-full rounded bg-muted/60" /></div><div className="mt-auto flex items-center justify-between border-t border-border pt-3"><span className="font-mono-app text-[8px] text-muted-foreground">CONTROLLED COPY · {document.hash}</span><span className="text-[9px] font-bold text-muted-foreground">PAGE 1 / 4</span></div></div></div><div className="space-y-4"><div><div className="text-[10px] font-extrabold uppercase tracking-[.14em] text-muted-foreground">Classification</div><div className="mt-2"><StatusBadge value={document.confidentiality} kind="confidentiality" /></div></div><div><div className="text-[10px] font-extrabold uppercase tracking-[.14em] text-muted-foreground">Integrity state</div><div className="mt-2 flex items-center gap-2 text-xs font-bold text-[#16803C]"><CheckCircle2 size={15} /> {document.integrity}</div></div><div><div className="text-[10px] font-extrabold uppercase tracking-[.14em] text-muted-foreground">Last accessed</div><div className="mt-1 text-xs font-semibold text-foreground">{formatDateTime(document.lastAccessed)}</div></div><button type="button" onClick={() => onDetails(document)} data-testid={`button-preview-details-${document.id}`} className="mt-3 inline-flex w-full items-center justify-center gap-2 rounded-md border border-border py-2 text-xs font-bold text-foreground hover:bg-muted">Open full details <ChevronRight size={14} /></button></div></div>
  </Modal>;
}

function DetailsDrawer({ document, onClose, onShare, onDownload, onVerify }) {
  return <div className="fixed inset-0 z-40 bg-foreground/20" data-testid="drawer-details-overlay"><aside className="shadow-2xl animate-enter absolute right-0 top-0 flex h-full w-full max-w-[470px] flex-col overflow-y-auto bg-card" data-testid={`drawer-document-details-${document.id}`}><div className="sticky top-0 z-10 flex items-start justify-between border-b border-border bg-card px-6 py-5"><div><div className="mb-1 font-mono-app text-[10px] font-bold tracking-wide text-primary">{document.id} · DOCUMENT RECORD</div><h2 className="max-w-[340px] text-lg font-extrabold leading-6 text-foreground">{document.documentName}</h2></div><IconButton label="Close details" onClick={onClose} testId="button-close-details-drawer" className="h-8 w-8 text-muted-foreground"><X size={18} /></IconButton></div><div className="space-y-6 p-6"><div className="flex flex-wrap gap-2"><StatusBadge value={document.status} /><StatusBadge value={document.integrity} kind="integrity" /><StatusBadge value={document.confidentiality} kind="confidentiality" /></div><div className="grid grid-cols-2 gap-x-5 gap-y-4 border-y border-border py-5">{[['Case ID', document.caseId], ['Document type', document.documentType], ['Uploaded by', document.uploadedBy], ['Upload date', formatDate(document.uploadDate)], ['Current version', `v${document.version}`], ['Last modified', formatDateTime(document.lastModified)], ['Total accesses', document.totalAccesses], ['Last accessed by', document.lastAccessedBy]].map(([label, value]) => <div key={label}><div className="text-[10px] font-extrabold uppercase tracking-[.11em] text-muted-foreground">{label}</div><div data-testid={`detail-${label.toLowerCase().replaceAll(' ', '-')}`} className={`mt-1 text-xs font-bold text-foreground ${label.includes('ID') ? 'font-mono-app' : ''}`}>{value}</div></div>)}</div><div className="rounded-lg border border-border bg-muted/25 p-3"><div className="mb-1 flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-[.12em] text-muted-foreground"><KeyRound size={13} /> Security information</div><div className="text-xs font-bold text-foreground">AES-256 encrypted</div><div className="mt-1 text-[11px] text-[#16803C]">Integrity {document.integrity.toLowerCase()} using SHA-256</div><div data-testid={`detail-hash-${document.id}`} className="font-mono-app mt-2 break-all text-[11px] text-foreground">{document.hash} 4cf7b1e2c9a0</div></div><div><div className="mb-3 flex items-center gap-2 text-[11px] font-extrabold uppercase tracking-[.13em] text-muted-foreground"><History size={14} /> Version history</div><div className="ml-1 border-l border-border">{document.versionHistory.map((item, index) => <div key={`${item.version}-${item.date}`} className="relative pb-4 pl-5 last:pb-0" data-testid={`timeline-version-${document.id}-${item.version}`}><span className={`absolute -left-[5px] top-0 h-2.5 w-2.5 rounded-full border-2 border-card ${index === 0 ? 'bg-primary' : 'bg-muted-foreground'}`} /><div className="flex items-center justify-between"><span className="font-mono-app text-[11px] font-bold text-foreground">Version {item.version}</span><span className="text-[10px] text-muted-foreground">{item.date}</span></div><div className="mt-1 text-[11px] text-muted-foreground">{item.note} · <span className="font-semibold text-foreground">{item.user}</span></div></div>)}</div></div></div><div className="mt-auto flex gap-2 border-t border-border p-5"><button type="button" onClick={() => onShare(document)} data-testid={`button-drawer-share-${document.id}`} className="inline-flex flex-1 items-center justify-center gap-2 rounded-md bg-primary hover:bg-[#123A61] py-2.5 text-xs font-bold text-primary-foreground transition-colors"><Share2 size={14} /> Secure share</button><button type="button" onClick={() => onDownload(document)} data-testid={`button-drawer-download-${document.id}`} className="inline-flex items-center justify-center gap-2 rounded-md border border-border px-3 py-2.5 text-xs font-bold text-foreground hover:bg-muted"><Download size={14} /></button><button type="button" onClick={() => onVerify(document)} data-testid={`button-drawer-verify-${document.id}`} className="inline-flex items-center justify-center gap-2 rounded-md border border-primary/30 px-3 py-2.5 text-xs font-bold text-primary hover:bg-secondary"><ShieldCheckIcon /><span className="sr-only">Verify integrity</span></button></div></aside></div>;
}

function ShieldCheckIcon() { return <CheckCircle2 size={16} />; }

function ShareModal({ document, onClose, onSuccess }) {
  const [recipient, setRecipient] = useState(''); const [permission, setPermission] = useState('View'); const [expiry, setExpiry] = useState('2026-09-30'); const [watermark, setWatermark] = useState(true); const [logAccess, setLogAccess] = useState(true);
  return <Modal title="Secure share" eyebrow="Controlled access" onClose={onClose} testId="modal-secure-share"><div className="px-6 py-5"><div className="mb-5 flex items-center gap-3 rounded-lg bg-secondary p-3"><div className="flex h-9 w-9 items-center justify-center rounded-md bg-card text-primary shadow-sm"><Link2 size={17} /></div><div className="min-w-0"><div className="truncate text-xs font-extrabold text-foreground">{document.documentName}</div><div className="font-mono-app text-[10px] text-muted-foreground">{document.caseId} · {document.confidentiality}</div></div></div><div className="space-y-4"><label className="block"><span className="mb-1.5 block text-xs font-bold text-foreground">Select User</span><input value={recipient} onChange={(event) => setRecipient(event.target.value)} data-testid="input-share-recipient" placeholder="name@justice.gov" className="h-10 w-full rounded-md border border-input bg-card px-3 text-sm text-foreground outline-none focus:border-primary" /></label><div className="grid grid-cols-2 gap-3"><label><span className="mb-1.5 block text-xs font-bold text-foreground">Permission</span><select value={permission} onChange={(event) => setPermission(event.target.value)} data-testid="select-share-permission" className="h-10 w-full rounded-md border border-input bg-card px-3 text-xs text-foreground outline-none focus:border-primary"><option>View</option><option>View + Download</option><option>Edit</option></select></label><label><span className="mb-1.5 block text-xs font-bold text-foreground">Access Expiry</span><input type="date" value={expiry} onChange={(event) => setExpiry(event.target.value)} data-testid="input-share-expiry" className="h-10 w-full rounded-md border border-input bg-card px-3 text-xs text-foreground outline-none focus:border-primary" /></label></div><div className="space-y-2 rounded-lg border border-border bg-muted/25 p-3"><label className="flex items-center gap-2 text-xs font-semibold text-foreground"><input type="checkbox" checked={watermark} onChange={(event) => setWatermark(event.target.checked)} data-testid="checkbox-share-watermark" />Watermark document</label><label className="flex items-center gap-2 text-xs font-semibold text-foreground"><input type="checkbox" checked={logAccess} onChange={(event) => setLogAccess(event.target.checked)} data-testid="checkbox-share-log-access" />Log all access</label></div></div><div className="mt-5 flex justify-end gap-2 border-t border-border pt-4"><button type="button" onClick={onClose} data-testid="button-cancel-share" className="rounded-md px-3 py-2 text-xs font-bold text-muted-foreground hover:bg-muted">Cancel</button><button type="button" onClick={() => onSuccess(recipient || 'secure recipient')} data-testid="button-confirm-share" className="inline-flex items-center gap-2 rounded-md bg-primary hover:bg-[#123A61] px-4 py-2 text-xs font-bold text-primary-foreground transition-colors"><Share2 size={14} /> Generate Secure Share</button></div></div></Modal>;
}

function DownloadModal({ document, onClose, onConfirm }) { return <Modal title="Confirm controlled download" eyebrow="Audit event" onClose={onClose} testId="modal-download-confirm"><div className="px-6 py-5"><div className="flex gap-3 rounded-lg border border-[#B77900]/30 bg-[#B77900]/10 p-4 text-[#B77900]"><AlertTriangle size={18} className="mt-0.5 shrink-0" /><div className="text-xs leading-5"><div className="font-extrabold">This action will be recorded.</div><div className="mt-1 text-foreground">A controlled copy of <strong>{document.documentName}</strong> will be prepared and this access will be added to the audit trail.</div></div></div><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} data-testid="button-cancel-download" className="rounded-md px-3 py-2 text-xs font-bold text-muted-foreground hover:bg-muted">Cancel</button><button type="button" onClick={onConfirm} data-testid="button-confirm-download" className="inline-flex items-center gap-2 rounded-md bg-primary hover:bg-[#123A61] px-4 py-2 text-xs font-bold text-primary-foreground transition-colors"><Download size={14} /> Confirm download</button></div></div></Modal>; }

function VerifyModal({ document, onClose }) { const [running, setRunning] = useState(true); useEffect(() => { const timer = setTimeout(() => setRunning(false), 1800); return () => clearTimeout(timer); }, []); return <Modal title="Integrity Verification" eyebrow="Cryptographic verification" onClose={onClose} testId="modal-verify-integrity"><div className="px-6 py-6">{running ? <div className="text-center"><div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-secondary text-primary"><Loader2 size={27} className="animate-spin" /></div><h3 className="text-sm font-extrabold text-foreground">Comparing fingerprints…</h3><p data-testid="status-verification-running" className="mt-1 text-xs text-muted-foreground">Checking {document.documentName} against the evidence ledger.</p></div> : <div><div className="mb-4 flex items-center gap-3 rounded-lg border border-[#16803C]/30 bg-[#16803C]/10 p-3 text-[#16803C]"><CheckCircle2 size={21} /><div><div data-testid="status-verification-verified" className="text-sm font-extrabold">Integrity Verified</div><div className="text-xs">No unauthorized modification detected.</div></div></div><dl className="space-y-3 rounded-lg border border-border bg-card p-4 text-xs"><div className="flex justify-between gap-4"><dt className="font-bold text-muted-foreground">Document</dt><dd className="text-right font-semibold text-foreground">{document.documentName}</dd></div><div className="flex justify-between gap-4"><dt className="font-bold text-muted-foreground">Original SHA-256</dt><dd className="font-mono-app text-right text-foreground">{document.hash} 4cf7b1e2c9a0</dd></div><div className="flex justify-between gap-4"><dt className="font-bold text-muted-foreground">Current SHA-256</dt><dd className="font-mono-app text-right text-foreground">{document.hash} 4cf7b1e2c9a0</dd></div></dl></div>}<button type="button" onClick={onClose} data-testid="button-close-verification" className="mt-5 block ml-auto rounded-md border border-border px-4 py-2 text-xs font-bold text-foreground hover:bg-muted">Close</button></div></Modal>; }

function Notifications({ onClose }) { return <Dropdown className="right-4 top-14 w-[310px]" data-testid="dropdown-notifications"><div className="flex items-center justify-between border-b border-border px-4 py-3"><span className="text-sm font-extrabold text-foreground">Notifications</span><span className="rounded-full bg-secondary px-2 py-0.5 text-[10px] font-bold text-primary">3 new</span></div>{[['Verification needed', 'Charging Memorandum is awaiting review', '8 min ago', 'amber'], ['New secure share', 'ADA N. Okafor accessed Evidence Inventory', '42 min ago', 'cyan'], ['System check complete', 'All evidence services operational', 'Today, 07:00', 'emerald']].map(([title, body, time, tone], index) => <button type="button" key={title} onClick={onClose} data-testid={`notification-${index}`} className="flex w-full gap-3 border-b border-border/50 px-4 py-3 text-left hover:bg-muted/50"><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${tone === 'amber' ? 'bg-[#B77900]' : tone === 'cyan' ? 'bg-primary' : 'bg-[#16803C]'}`} /><span><span className="block text-xs font-extrabold text-foreground">{title}</span><span className="mt-0.5 block text-[11px] text-muted-foreground">{body}</span><span className="mt-1 block text-[10px] text-muted-foreground/70">{time}</span></span></button>)}</Dropdown>; }

function ProfileMenu({ onClose }) { return <Dropdown className="right-4 top-14 w-56" data-testid="dropdown-profile"><div className="border-b border-border px-4 py-3"><div className="text-xs font-extrabold text-foreground">Officer Raj Patel</div><div className="mt-0.5 text-[10px] text-muted-foreground">raj.patel@securedocs.gov</div></div><button type="button" onClick={onClose} data-testid="profile-settings" className="flex w-full items-center gap-2 px-4 py-3 text-xs font-bold text-foreground hover:bg-muted"><UserRound size={14} />Profile & preferences</button><button type="button" onClick={onClose} data-testid="profile-sign-out" className="flex w-full items-center gap-2 border-t border-border px-4 py-3 text-xs font-bold text-muted-foreground hover:bg-muted"><LogOutIcon />Sign out</button></Dropdown>; }
function LogOutIcon() { return <LogOut size={14} />; }

export default function AllDocuments() {
  const [collapsed, setCollapsed] = useState(false); const [query, setQuery] = useState(''); const [filters, setFilters] = useState({ caseId: '', documentType: '', uploadedBy: '', dateFrom: '', dateTo: '', status: '', integrity: '', confidentiality: '' }); const [applied, setApplied] = useState(filters); const [sort, setSort] = useState({ key: 'lastModified', direction: 'desc' }); const [page, setPage] = useState(1); const [menu, setMenu] = useState(null); const [modal, setModal] = useState(null); const [toast, setToast] = useState(''); const [notifications, setNotifications] = useState(false); const [profile, setProfile] = useState(false); const [loading, setLoading] = useState(false); const [docs, setDocs] = useState(seedDocuments); const perPage = 7;

  useEffect(() => {
    let active = true;
    fetch('/api/documents')
      .then((res) => (res.ok ? res.json() : null))
      .then((json) => {
        if (active && json?.success && Array.isArray(json.data) && json.data.length > 0) {
          const apiDocs = json.data.map((d, index) => ({
            id: d._id ? `SD-${d._id.slice(-6)}` : `SD-API-${index}`,
            documentName: d.fileName || 'document.pdf',
            caseId: d.caseId || 'CASE-2026-001',
            documentType: d.documentType || 'FIR',
            uploadedBy: d.uploadedBy || 'Officer Raj Patel',
            uploadDate: d.createdAt ? String(d.createdAt).slice(0, 10) : '2026-09-02',
            version: 1,
            status: d.status || 'Approved',
            integrity: 'Verified',
            confidentiality: 'Confidential',
            lastModified: d.updatedAt || d.createdAt || new Date().toISOString(),
            hash: d.sha256Hash || 'sha256: 7f0c…d91a',
            lastAccessed: new Date().toISOString(),
            totalAccesses: 1,
            lastAccessedBy: 'Officer Raj Patel',
            versionHistory: [{ version: 1, date: 'Sep 02, 2026 · 12:00', user: d.uploadedBy || 'Officer', note: 'Initial upload' }],
          }));
          const existingIds = new Set(apiDocs.map((doc) => doc.documentName.toLowerCase()));
          const remainingSeed = seedDocuments.filter((doc) => !existingIds.has(doc.documentName.toLowerCase()));
          setDocs([...apiDocs, ...remainingSeed]);
        }
      })
      .catch((err) => {
        console.warn('Could not fetch documents from /api/documents:', err);
      });
    return () => { active = false; };
  }, []);

  const filtered = useMemo(() => docs.filter((doc) => { const q = query.toLowerCase(); const matchesQuery = !q || [doc.documentName, doc.caseId, doc.uploadedBy, doc.documentType, doc.id].some((value) => value.toLowerCase().includes(q)); const inDateRange = (!applied.dateFrom || doc.uploadDate >= applied.dateFrom) && (!applied.dateTo || doc.uploadDate <= applied.dateTo); return matchesQuery && inDateRange && Object.entries(applied).filter(([key]) => !['dateFrom', 'dateTo'].includes(key)).every(([key, value]) => !value || String(doc[key]).toLowerCase().includes(value.toLowerCase())); }), [docs, query, applied]);
  const sorted = useMemo(() => [...filtered].sort((a, b) => { if (sort.key === 'version') return sort.direction === 'asc' ? a.version - b.version : b.version - a.version; const av = String(a[sort.key]).toLowerCase(); const bv = String(b[sort.key]).toLowerCase(); return sort.direction === 'asc' ? av.localeCompare(bv) : bv.localeCompare(av); }), [filtered, sort]);
  const totalPages = Math.max(1, Math.ceil(sorted.length / perPage)); const pageDocs = sorted.slice((page - 1) * perPage, page * perPage);
  const changeSort = (key) => { setSort((current) => ({ key, direction: current.key === key && current.direction === 'asc' ? 'desc' : 'asc' })); setPage(1); };
  const clearFilters = () => { const blank = { caseId: '', documentType: '', uploadedBy: '', dateFrom: '', dateTo: '', status: '', integrity: '', confidentiality: '' }; setFilters(blank); setApplied(blank); setQuery(''); setPage(1); };
  const openAction = (doc, action) => { setMenu(null); setModal({ type: action, document: doc }); };
  const menuApi = (id) => { const fn = (doc) => setMenu((current) => current === id ? null : id); fn.activeId = menu; fn.share = (doc) => openAction(doc, 'share'); fn.download = (doc) => openAction(doc, 'download'); fn.verify = (doc) => openAction(doc, 'verify'); return fn; };
  const handleUpload = async (info) => {
    const docName = info.name || info.fileName || 'secure-document.pdf';
    const cId = info.caseId || 'CASE-2026-NEW';
    try {
      await fetch('/api/documents', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          caseId: cId,
          fileName: docName,
          documentType: info.type || 'FIR',
          description: info.name,
          uploadedBy: 'Officer Raj Patel',
          status: 'Approved',
        }),
      });
    } catch (err) {
      console.warn('API document upload failed, saving to state:', err);
    }
    const newDoc = { ...seedDocuments[0], id: `SD-${Math.floor(26100 + Math.random() * 80)}`, documentName: docName, caseId: cId, documentType: info.type, confidentiality: info.confidentiality, uploadDate: '2026-09-02', lastModified: '2026-09-02T16:50:00', uploadedBy: 'Officer Raj Patel', hash: 'sha256: 1a6e…9f22', version: 1, totalAccesses: 0, lastAccessed: '2026-09-02T16:50:00', lastAccessedBy: 'Officer Raj Patel', versionHistory: [{ version: 1, date: 'Sep 02, 2026 · 16:50', user: 'Officer Raj Patel', note: 'Secure intake complete' }] }; setDocs((current) => [newDoc, ...current]); setModal(null); setToast('Document uploaded securely.');
  };
  const handleDownload = () => { setModal(null); setToast('Controlled copy prepared for download'); };
  const activeFilterCount = Object.values(applied).filter(Boolean).length;
  const columns = [['documentName', 'Document Name'], ['caseId', 'Case ID'], ['documentType', 'Document Type'], ['uploadedBy', 'Uploaded By'], ['uploadDate', 'Upload Date'], ['version', 'Version'], ['status', 'Status'], ['integrity', 'Integrity'], ['confidentiality', 'Confidentiality']];
  return <>
    <div className="mx-auto max-w-[1600px] py-2 sm:py-4">
    <div className="mb-6 flex flex-col justify-between gap-4 xl:flex-row xl:items-end"><div><div className="mb-2 flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-[.18em] text-primary"><span className="h-1.5 w-1.5 rounded-full bg-primary" />Records workspace / control room</div><h1 data-testid="text-page-title" className="text-[28px] font-extrabold tracking-[-.04em] text-foreground sm:text-[32px]">All Documents</h1><p data-testid="text-page-subtitle" className="mt-1 text-sm text-muted-foreground">Securely manage, search and monitor legal and investigation documents.</p></div><div className="flex items-center gap-2"><div className="hidden items-center gap-2 rounded-md border border-[#16803C]/30 bg-[#16803C]/10 px-3 py-2 text-[11px] font-bold text-[#16803C] sm:flex" data-testid="status-trust-indicator"><CheckCircle2 size={14} /> Chain of custody operational</div><button type="button" onClick={() => setModal({ type: 'upload' })} data-testid="button-open-upload" className="inline-flex items-center gap-2 rounded-md bg-primary hover:bg-[#123A61] px-3.5 py-2.5 text-xs font-extrabold text-primary-foreground shadow-sm transition-colors"><Plus size={15} /> Upload Document</button></div></div>
       <section className="mb-6 grid grid-cols-2 gap-3 lg:grid-cols-4" data-testid="security-summary"><SummaryCard label="Total Documents" value="1,248" detail="Across all active matters" icon={FileCheck2} tone="navy" testId="summary-total-documents" /><SummaryCard label="Pending Review" value="86" detail="Awaiting officer or legal review" icon={AlertTriangle} tone="amber" testId="summary-pending-review" /><SummaryCard label="Integrity Issues" value="7" detail="Records requiring verification" icon={ShieldAlert} tone="red" testId="summary-integrity-issues" /><SummaryCard label="Restricted Documents" value="142" detail="Highest access controls" icon={Shield} tone="teal" testId="summary-restricted-documents" /></section>
       <section className="overflow-hidden rounded-lg border border-border bg-card shadow-sm" data-testid="documents-workspace"><div className="border-b border-border px-4 pt-4 sm:px-5"><div className="flex flex-col gap-3 xl:flex-row xl:items-center"><div className="relative min-w-0 flex-1"><Search size={16} className="absolute left-3 top-2.5 text-muted-foreground" /><input type="search" value={query} onChange={(event) => { setQuery(event.target.value); setPage(1); }} data-testid="input-search-documents" placeholder="Search by document name, Case ID, officer..." className="h-9 w-full rounded-md border border-input bg-background pl-9 pr-9 text-xs font-medium text-foreground outline-none placeholder:text-muted-foreground/60 focus:border-primary" />{query && <IconButton label="Clear search" onClick={() => setQuery('')} testId="button-clear-search" className="absolute right-1 top-1 h-7 w-7 text-muted-foreground"><X size={14} /></IconButton>}</div><div className="flex items-center gap-2"><button type="button" onClick={() => setLoading(true) || setTimeout(() => setLoading(false), 500)} data-testid="button-refresh-documents" className="inline-flex h-9 items-center gap-2 rounded-md border border-border bg-card px-3 text-xs font-bold text-foreground hover:bg-muted"><RefreshCw size={14} className={loading ? 'animate-spin' : ''} />Refresh</button><button type="button" onClick={() => {}} data-testid="button-bulk-actions" className="inline-flex h-9 items-center gap-2 rounded-md border border-border bg-card px-3 text-xs font-bold text-foreground hover:bg-muted"><SlidersHorizontal size={14} />Bulk actions</button></div></div><div className="mt-4 grid gap-3 pb-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6"><SelectField label="Case ID" value={filters.caseId} onChange={(value) => setFilters({ ...filters, caseId: value })} options={unique('caseId')} testId="select-filter-case-id" /><SelectField label="Document Type" value={filters.documentType} onChange={(value) => setFilters({ ...filters, documentType: value })} options={filterOptions.documentType} testId="select-filter-document-type" /><SelectField label="Uploaded By" value={filters.uploadedBy} onChange={(value) => setFilters({ ...filters, uploadedBy: value })} options={filterOptions.uploadedBy} testId="select-filter-uploader" /><SelectField label="Status" value={filters.status} onChange={(value) => setFilters({ ...filters, status: value })} options={filterOptions.status} testId="select-filter-status" /><SelectField label="Integrity" value={filters.integrity} onChange={(value) => setFilters({ ...filters, integrity: value })} options={filterOptions.integrity} testId="select-filter-integrity" /><SelectField label="Confidentiality" value={filters.confidentiality} onChange={(value) => setFilters({ ...filters, confidentiality: value })} options={filterOptions.confidentiality} testId="select-filter-confidentiality" /></div><div className="flex flex-wrap items-center gap-2 border-t border-border py-3"><Filter size={14} className="text-muted-foreground" /><span className="text-[11px] font-bold text-muted-foreground">Date</span><input type="date" value={filters.dateFrom} onChange={(event) => setFilters({ ...filters, dateFrom: event.target.value })} data-testid="input-filter-date-from" className="h-8 rounded-md border border-input bg-card px-2 text-[11px] text-foreground outline-none focus:border-primary" /><span className="text-xs text-muted-foreground">to</span><input type="date" value={filters.dateTo} onChange={(event) => setFilters({ ...filters, dateTo: event.target.value })} data-testid="input-filter-date-to" className="h-8 rounded-md border border-input bg-card px-2 text-[11px] text-foreground outline-none focus:border-primary" /><button type="button" onClick={() => { setApplied(filters); setPage(1); }} data-testid="button-apply-filters" className="ml-auto inline-flex h-8 items-center gap-1.5 rounded-md bg-primary hover:bg-[#123A61] px-3 text-[11px] font-extrabold text-primary-foreground transition-colors">Apply Filters {activeFilterCount > 0 && <span className="rounded bg-white/20 px-1.5">{activeFilterCount}</span>}</button><button type="button" onClick={clearFilters} data-testid="button-clear-filters" className="h-8 rounded-md px-2 text-[11px] font-bold text-muted-foreground hover:bg-muted">Clear Filters</button></div></div>
        <div className="flex items-center justify-between border-b border-border px-5 py-3"><div className="text-xs font-bold text-foreground"><span data-testid="text-result-count">{sorted.length}</span> records <span className="font-normal text-muted-foreground">matching current view</span></div><div className="hidden items-center gap-1.5 text-[10px] font-bold uppercase tracking-[.12em] text-muted-foreground sm:flex"><LockIcon /> Restricted workspace</div></div>
         <div className="scrollbar-thin overflow-x-auto">{loading ? <SkeletonRows /> : pageDocs.length === 0 ? <EmptyState onClear={clearFilters} /> : <table className="w-full min-w-[1420px] border-collapse text-left"><thead><tr className="bg-muted/40 font-mono text-[9px] uppercase tracking-[.12em] text-muted-foreground">{[['', ''], ...columns, ['', 'Actions']].map(([key, label], index) => <th key={`${label}-${index}`} className="whitespace-nowrap px-3 py-2.5 font-bold first:px-3">{key && ['documentName', 'caseId', 'uploadDate', 'version', 'status'].includes(key) ? <button type="button" onClick={() => changeSort(key)} data-testid={`button-sort-${key}`} className="inline-flex items-center gap-1 hover:text-primary">{label}{sort.key === key ? sort.direction === 'asc' ? <ArrowUp size={12} /> : <ArrowDown size={12} /> : <span className="text-muted-foreground/40">↕</span>}</button> : label}</th>)}</tr></thead><tbody>{pageDocs.map((document) => <DocumentRow key={document.id} document={document} onMenu={Object.assign(menuApi(document.id), { activeId: menu, share: (doc) => openAction(doc, 'share'), download: (doc) => openAction(doc, 'download'), verify: (doc) => openAction(doc, 'verify') })} onPreview={(doc) => setModal({ type: 'preview', document: doc })} onDetails={(doc) => { setMenu(null); setModal({ type: 'details', document: doc }); }} />)}</tbody></table>}</div>
        <div className="flex flex-col gap-3 border-t border-border px-5 py-3 sm:flex-row sm:items-center sm:justify-between"><div className="text-[11px] text-muted-foreground">Showing <strong className="text-foreground">{sorted.length ? (page - 1) * perPage + 1 : 0}–{Math.min(page * perPage, sorted.length)}</strong> of <strong className="text-foreground">{sorted.length}</strong> records</div><div className="flex items-center gap-1"><button type="button" disabled={page === 1} onClick={() => setPage((value) => Math.max(1, value - 1))} data-testid="button-pagination-previous" className="flex h-7 w-7 items-center justify-center rounded-md border border-border text-muted-foreground hover:bg-muted disabled:cursor-not-allowed disabled:opacity-40"><ChevronLeft size={14} /></button>{Array.from({ length: totalPages }, (_, index) => index + 1).slice(0, 5).map((number) => <button type="button" key={number} onClick={() => setPage(number)} data-testid={`button-pagination-${number}`} className={`h-7 min-w-7 rounded-md border px-2 text-[11px] font-bold transition-colors ${page === number ? 'border-primary bg-primary text-primary-foreground' : 'border-border text-foreground hover:bg-muted'}`}>{number}</button>)}<button type="button" disabled={page === totalPages} onClick={() => setPage((value) => Math.min(totalPages, value + 1))} data-testid="button-pagination-next" className="flex h-7 w-7 items-center justify-center rounded-md border border-border text-muted-foreground hover:bg-muted disabled:cursor-not-allowed disabled:opacity-40"><ChevronRight size={14} /></button></div></div>
      </section><div className="mt-5 flex items-center gap-2 text-[10px] font-medium text-muted-foreground"><CircleHelp size={13} /> Every access, version change, and share event is retained in the immutable audit ledger.</div></div>
    {modal?.type === 'upload' && <UploadModal onClose={() => setModal(null)} onUpload={handleUpload} />}{modal?.type === 'preview' && <PreviewModal document={modal.document} onClose={() => setModal(null)} onDetails={(doc) => setModal({ type: 'details', document: doc })} />}{modal?.type === 'details' && <DetailsDrawer document={modal.document} onClose={() => setModal(null)} onShare={(doc) => setModal({ type: 'share', document: doc })} onDownload={(doc) => setModal({ type: 'download', document: doc })} onVerify={(doc) => setModal({ type: 'verify', document: doc })} />}{modal?.type === 'share' && <ShareModal document={modal.document} onClose={() => setModal(null)} onSuccess={(recipient) => { setModal(null); setToast(`Secure link created for ${recipient}`); }} />}{modal?.type === 'download' && <DownloadModal document={modal.document} onClose={() => setModal(null)} onConfirm={handleDownload} />}{modal?.type === 'verify' && <VerifyModal document={modal.document} onClose={() => setModal(null)} />}{toast && <Toast message={toast} onClose={() => setToast('')} />}</>;
}

function LockIcon() { return <span className="inline-flex h-4 w-4 items-center justify-center rounded-sm bg-secondary text-primary"><Shield size={10} /></span>; }